<ul class="nav nav-pills">
  <li class="active"><a href="#">Fields</a></li>

  <div class="btn-group pull-right">
    <%= link_to "Add Field", "#fields_new_modal", class: "admin_modal btn btn-primary", role: "button", data: { toggle: "modal" } %>
    <%= render partial: "super/layouts/buttons" %>
  </div>
</ul>

<div class="row">
  <div class="col-md-12">
    <h3>Fields</h3>
    <% if @fields.empty? %>
      <p>There aren't any yet!</p>
    <% else %>
      <%= render partial: "super/fields/table", locals: { fields: @fields } %>
    <% end %>
  </div>
</div>

<%= render partial: "super/fields/new_modal" %>
<%= render partial: "super/users/assign_modal" %>
<%= render partial: "super/users/trade_modal" %>
