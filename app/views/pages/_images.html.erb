<div class="owl-wrapper">
  <div class="owl-carousel">
    <% Image.all.each do |image| %>
      <%= link_to image.link do %>
        <%= image_tag image.src %>
      <% end %>
    <% end %>
  </div>
</div>
