<table class="data-table table table-striped">
  <thead>
    <tr>
      <th>League</th>
      <th>Date</th>
      <th>Time</th>
      <th>Field</th>
      <th>Teams</th>
      <th class="dont-sort"></th>
    </tr>
  </thead>
  <tbody>
    <% games.each do |game| %>
      <tr>
        <td><%= game.league.name %></td>
        <td><%= game.date %></td>
        <td><%= game.time %></td>
        <td><%= game.field.name %></td>
        <td><%= game.name %></td>
        <td class="btn-group">
          <%= link_to "Stats", "#", class: "btn btn-success edit-stats", data: { id: game.id } %>
          <%= link_to "Edit", edit_admin_game_path(game), class: "btn btn-primary" %>
          <%= link_to "Delete", admin_game_path(game), method: :delete, class: "btn btn-danger", confirm: "Are you sure you want to delete this game?" %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
